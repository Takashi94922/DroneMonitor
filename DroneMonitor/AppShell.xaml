<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:DroneMonitor.Views"
    x:Class="DroneMonitor.AppShell">

    <Shell.TitleView>
        <Grid Padding="10" ColumnDefinitions="*,Auto">
            <Label Text="Drone Monitor" FontSize="20" VerticalOptions="Center" />
            <Button x:Name="bleConnectBtn"
                    Text="BLE接続"
                    Clicked="OnBleConnectClicked"
                    Grid.Column="1"
                    VerticalOptions="Center"
                    Margin="0,0,10,0"/>
        </Grid>
    </Shell.TitleView>

    <TabBar>
        <ShellContent Title="Home" ContentTemplate="{DataTemplate views:HomePage}" />
        <ShellContent Title="Dashboard" ContentTemplate="{DataTemplate views:DashboardPage}" />
        <ShellContent Title="Notification" ContentTemplate="{DataTemplate views:NotificationPage}" />
    </TabBar>
</Shell>
